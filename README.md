# homebrew-tap

Homebrew tap for [`pwsh-autoupdate`](https://github.com/mericstam/pwsh-autoupdate).

```sh
brew install mericstam/tap/pwsh-autoupdate
```

The formula is regenerated per release by the upstream repo's release workflow.
